# Plan: パスワード生成 Web アプリ

## 概要
スタンドアロン HTML/CSS/Vanilla JS 製のパスワード生成アプリ。  
サービスごとに使用できる記号が違う問題を解決するため、記号のプリセット（サービス別 + 汎用）と手動チェックボックスを組み合わせた UI を提供する。

---

## ファイル構成
```
password-generator/
├── index.html          # メインHTML・全 UI 構造
├── css/
│   └── style.css       # ライト / ダークテーマ デザイン（SCSS コンパイル済み）
├── scss/
│   ├── style.scss      # エントリポイント
│   ├── _variables.scss # テーマトークン（ライト :root + ダーク [data-theme=dark]）
│   └── ...             # パーシャル各種
└── js/
    ├── presets.js      # プリセット定義データ（サービス別・汎用）
    ├── generator.js    # パスワード生成ロジック（Web Crypto API）
    └── app.js          # UI イベント制御・強度計算・コピー処理・テーマ切り替え
```

---

## 機能仕様

### 文字種設定
- 大文字 (A-Z) オン/オフ トグル
- 小文字 (a-z) オン/オフ トグル
- 数字 (0-9) オン/オフ トグル
- 記号オン/オフ（記号セクションの一括切替）

### 記号プリセット
**汎用カテゴリ（常時表示）**
- 全記号: `!"#$%&'()*+,-./:;<=>?@[\]^_{|}~`
- 標準セット: `!@#$%^&*()-_=+`
- シンプル: `!@#$%`
- 記号なし（数字・英字のみ）

**サービス別プリセット（ドロップダウン）**
- Amazon
- Google
- GitHub
- Apple/iCloud
- Microsoft
- Twitter/X
- LINE
※ ポリシー変更の可能性があるため「参考値」として注釈を表示

プリセット選択すると、下の記号チェックボックスが自動で ON/OFF になる。

### 記号個別チェックボックス
全記号をグリッド表示（カテゴリ分け）
1. `! @ # $ % ^ & *` — 上段記号
2. `( ) - _ + = [ ] { }` — 括弧・算術
3. `| ; : ' " , . / < > ? \` — 区切り

### パスワード長
- スライダー: 8〜64
- 数値入力（連動）
- デフォルト: 16

### 除外文字
- テキスト入力で除外文字を自由指定
- プリセットボタン「紛らわしい文字を除く」 → `0 O l 1 I | !` を自動セット

### 生成件数
- セレクト or スピナー: 1〜5件

### 生成ボタン
- `window.crypto.getRandomValues()` で暗号学的に安全な乱数を使用
- 各種設定で有効な文字プールを構築し、プールなければエラー表示

### 結果エリア
- 最大5件のカード形式表示
- 各カードにコピーボタン（Clipboard API）
- コピー後「コピー済！」フィードバック表示
- 強度インジケーター（弱/普通/強/非常に強い）

### 強度計算ロジック
- 文字プールのサイズ（種類数）× パスワード長でエントロピー計算
- 4段階表示（色付きバー）

---

## デザイン方針
- ダークテーマ（背景: #0f0f1a 系、アクセント: グラデーション紫〜青）
- カードレイアウト（設定パネル + 結果エリアの2カラム or 1カラム）
- トグルスイッチ、スライダー、チェックボックスをカスタムスタイリング
- コピー・生成時に軽いアニメーション

---

## セキュリティ
- 乱数生成: `window.crypto.getRandomValues()` のみ使用（Math.random 禁止）
- クライアントサイドのみで完結（サーバー通信なし）
- Clipboard API: 非同期 navigator.clipboard.writeText() を使用

---

## 実装ステップ
1. `presets.js` — プリセットデータを定義
2. `generator.js` — 文字プール構築・パスワード生成・強度計算
3. `index.html` — UI 構造作成
4. `style.css` — ダークテーマ・モダンデザイン
5. `app.js` — イベントバインド、UI 連動ロジック、コピー処理
6. 手動テスト（各プリセット・除外文字・全設定組み合わせ）

---

## スコープ外
- バックエンド処理（PHP 等）
- パスワード保存・履歴機能
- ユーザーアカウント
- ダークモード/ライトモード切替（ダーク固定）
